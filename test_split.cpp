/*
CSCI 104: Homework 1 Problem 1

Use this file to test your split.cpp file.

Create linked lists and split them with your split() function.

You can compile this file like this:
g++ split.cpp test_split.cpp -o test_split
*/

#include "split.h"
#include <cstdlib>
#include <cstddef>
#include <iostream>

using namespace std;


int main()
{
  // // Node* previousNode = NULL;

  // // for(int i = 0; i < argc; i++){
  // //   Node* tempNode = new Node;
  // //   tempNode->next = previousNode;
  // //   tempNode->value = atoi(argv[i]);
  // //   previousNode = tempNode;
  // // }

  // // Node* odds = NULL;
  // // Node* evens = NULL;
  
  // Node* node5 = new Node(10, nullptr);
  // Node* node4 = new Node(8, node5);
  // Node* node3 = new Node(6, node4);
  // Node* node2 = new Node(4, node3);
  // Node* node1 = new Node(2, node2);

  // Node* odds = nullptr;
  // Node* evens = nullptr;

  // cout << "Splitting The List" << endl;

  // split(node1, odds, evens);




  // cout << evens->value << endl;
  // cout << evens->next->value << endl;
  // cout << evens->next->next->value << endl;
  // cout << evens->next->next->next->value << endl;
  // // cout << evens->next->next->next->next->value << endl;
  // cout << node1 << endl;

  // delete node5;
  // delete node4;
  // delete node3;
  // delete node2;
  // delete node1;



  return 0;
}

